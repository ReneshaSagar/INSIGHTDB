[project]
name = "insightdb"
version = "0.1.0"
dependencies = [
    "flask",
    "pandas",
    "openai",
    "python-dotenv",
    "flask-cors",
    "gunicorn",
    "google-auth",
    "requests"
]

[tool.vercel]
entrypoint = "backend.app:app"
